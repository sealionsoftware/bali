

class ISOCalendarSystem implements CalendarSystem {

	//
	method Date getDate(Instant instant) {

		Number remaining = instant
		Number daysInYear = 365;
		Number year;
		while (){



		}



	}

	method Time getTime(Instant instant) {
		return null;
	}

	method Instant getInstant(Date date) {
		return getInstant(date, null);
	}

	method Instant getInstant(Date date, Time time) {

		long ret = 0;
		int years = date.getYear().subtract();

		return null;
	}
}
