import bali.Number
import bali.Integer
import bali.Iterable
import bali.String
import bali.Executable
import bali.Count
import bali.Counter

object CompoundExecutable implements Executable {

	field Count? i

 	method execute(){

	    i = new Counter();

//      TODO: get this working again

//		while (++i < 10) if ([3,5,7,8].contains(#i)) {
//			CONSOLE << ("Hi" + VALUE_FORMATTER.format(#i + 1));
//		}
 	}
}
