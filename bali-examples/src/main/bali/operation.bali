import bali.Console;
import bali.Number;
import bali.String;
import bali.Executable;

class OperationDoer() implements Executable {

 	method execute(){
 	    Number n = 1 + 2;
 	    String s = NUMBER_FACTORY.format(n) + "!";
 	    CONSOLE << s;

 	    if (true & (false | (true ^ false))){
 	        CONSOLE << "evaluated compound expression to true";
 	    }
 	}



}
