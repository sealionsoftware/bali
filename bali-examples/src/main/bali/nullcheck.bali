import bali.String
import bali.Executable

object NullChecker implements Executable {

	field String? greeting = "Hello World"

	method execute(){

		if (?greeting){
			CONSOLE << greeting;
		}
	}
}