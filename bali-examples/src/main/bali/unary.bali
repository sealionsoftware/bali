import bali.Number
import bali.String
import bali.Executable

object UnaryExecutable implements Executable {

 	method execute(){

 	    Number n = 1;
 	    n = -n; // Now -1
 	    n = |n; // Now 1

 	    CONSOLE << "Calc: " + VALUE_FORMATTER.format(n);

 	}

}
