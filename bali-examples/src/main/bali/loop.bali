import bali.Console;
import bali.Number;

interface Doer {
 	declare goDo();
}

class WhileDoer() implements Doer {

 	method goDo(){

		Number flag = 0;

	    while (flag.lessThan(4)){
	        CONSOLE.writeLine("Hello");
	        flag = flag.add(1);
	    }
 	}

}
