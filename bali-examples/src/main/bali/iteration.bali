import bali.Console;
import bali.Number;
import bali.Iterable;
import bali.String;
import bali.Iterator;

constant Iterable<String> NAMES = ["Tom", "Dick", "Harry", "George"];

interface Doer {
 	declare goDo();
}

class IterationDoer() implements Doer {

 	method goDo(){
		Iterator<String> i = NAMES.iterator();
	    while (i.hasNext()){
	        CONSOLE.printLine(i.next());
	    }
 	}

}
