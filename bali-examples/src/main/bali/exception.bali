import bali.Console;
import bali.Boolean;
import bali.String;
import bali.Executable;
import bali.Exception;

class TryDoer() implements Executable {

 	method execute(){

		try {
			throw new Exception("Test");
		} catch (Exception e){
            throw new Exception(e.getProblem() + " 2");
        } catch (Exception e){
            CONSOLE << e.getProblem();
        }

        throw new Exception("Uncaught");
 	}

}
