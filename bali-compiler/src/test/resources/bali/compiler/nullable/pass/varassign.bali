import bali.String
import bali.Executable

object Test implements Executable {

 	method execute {
 	    String? one = "G'Day"
 	    String? two
 	    if (?one) {
 	        String safe = one
 	    }
	    if (?one & true) {
            String safe = one
        }
        if (?one & ?two) {
            String safe = two
        }
 	}

}
